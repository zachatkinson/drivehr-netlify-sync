name: CI/CD Pipeline

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main, develop]
  schedule:
    # Run security audits daily at 2 AM UTC
    - cron: '0 2 * * *'

permissions:
  contents: read
  security-events: write
  actions: read

env:
  NODE_VERSION: '20'

jobs:
  # Code Quality and Security Checks
  quality:
    name: Code Quality & Security
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Type checking
        run: pnpm run type-check

      - name: Linting
        run: pnpm run lint

      - name: Format checking
        run: pnpm run format:check

      - name: Security audit
        run: pnpm audit --audit-level moderate

      - name: Dependency vulnerability check
        uses: actions/dependency-review-action@v4
        if: github.event_name == 'pull_request'

  # Unit and Integration Tests
  test:
    name: ${{ matrix.test-suite.name }}
    runs-on: ubuntu-latest
    needs: quality

    strategy:
      matrix:
        test-suite:
          - name: 'Config'
            pattern: 'test/lib/config.test.ts'
          - name: 'HTTP Client'
            pattern: 'test/lib/http-client.test.ts'
          - name: 'Environment Utilities'
            pattern: 'test/lib/env.test.ts'
          - name: 'Logger'
            pattern: 'test/lib/logger.test.ts'
          - name: 'Utils'
            pattern: 'test/lib/utils.test.ts'
          - name: 'Job Fetch Utils'
            pattern: 'test/lib/job-fetch-utils.test.ts'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'
          cache-dependency-path: |
            pnpm-lock.yaml
            package.json

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Run ${{ matrix.test-suite.name }}
        run: pnpm exec vitest run ${{ matrix.test-suite.pattern }} --coverage
        env:
          NODE_ENV: test

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage/lcov.info
          flags: unittests
          name: ${{ matrix.test-suite.name }}
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: true

      - name: Upload test results to Codecov
        if: ${{ !cancelled() }}
        uses: codecov/test-results-action@v1
        with:
          token: ${{ secrets.CODECOV_TOKEN }}

  # Build Verification
  build:
    name: Build Verification
    runs-on: ubuntu-latest
    needs: [quality, test]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build project
        run: pnpm run build

      - name: Verify build output
        run: |
          if [ ! -f "dist/functions/sync-jobs.js" ]; then
            echo "Build output missing - sync-jobs.js not found"
            exit 1
          fi
          if [ ! -f "dist/netlify.toml" ]; then
            echo "Build output missing - netlify.toml not found"
            exit 1
          fi
          echo "Build verification successful"

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: build-artifacts
          path: dist/
          retention-days: 7

  # Security Scanning
  security:
    name: Security Scanning
    runs-on: ubuntu-latest
    needs: quality

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Run npm audit for vulnerabilities
        run: pnpm audit --audit-level moderate || echo "Security audit completed with warnings"

      - name: CodeQL Analysis
        uses: github/codeql-action/init@v3
        with:
          languages: javascript

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v3
        with:
          category: '/language:javascript'
          upload: true

      - name: GitHub Advanced Security - Secret Scanning
        run: |
          echo "Secret scanning is automatically enabled for private repos with GitHub Advanced Security"
          echo "For additional secret patterns, configure .gitleaks.toml or similar tools"

  # Deployment (only on main branch)
  deploy:
    name: Deploy to Netlify
    runs-on: ubuntu-latest
    needs: [quality, test, build, security]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    environment:
      name: production
      url: ${{ steps.deploy.outputs.url }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build for production
        run: pnpm run build
        env:
          NODE_ENV: production

      - name: Deploy to Netlify
        id: deploy
        run: |
          echo "🚀 Deploying with pnpm exec (proper package manager)"
          echo "📦 Netlify CLI version:"
          pnpm exec netlify --version
          echo "📁 Checking dist directory contents:"
          ls -la dist/
          echo "📋 Checking netlify.toml in dist:"
          cat dist/netlify.toml
          echo "🔧 Using deployment flags: --prod --dir=dist --skip-functions-cache"
          echo "📂 Checking functions directory in dist:"
          ls -la dist/functions/
          echo "🔍 Attempting deployment without explicit functions flag (using netlify.toml config)"
          deployment=$(pnpm exec netlify deploy --prod --dir=dist --skip-functions-cache --json)
          url=$(echo "$deployment" | jq -r '.deploy_url // .url // ""')
          echo "url=$url" >> $GITHUB_OUTPUT
        env:
          NETLIFY_AUTH_TOKEN: ${{ secrets.NETLIFY_AUTH_TOKEN }}
          NETLIFY_SITE_ID: ${{ secrets.NETLIFY_SITE_ID }}

      - name: Notify deployment success
        if: success()
        run: |
          echo "✅ Deployment successful to: ${{ steps.deploy.outputs.url }}"

      - name: Notify deployment failure
        if: failure()
        run: |
          echo "❌ Deployment failed"
          exit 1

  # Performance and Health Checks
  health-check:
    name: Health Check
    runs-on: ubuntu-latest
    needs: deploy
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Wait for deployment to be ready
        run: sleep 30

      - name: Health check
        run: |
          DEPLOY_URL="${{ steps.deploy.outputs.url }}"
          if [ -z "$DEPLOY_URL" ]; then
            echo "No deploy URL available, skipping health check"
            exit 0
          fi

          echo "Testing health endpoint: $DEPLOY_URL/.netlify/functions/sync-jobs"
          response=$(curl -s -o /dev/null -w "%{http_code}" "$DEPLOY_URL/.netlify/functions/sync-jobs" || echo "000")

          if [ "$response" = "200" ]; then
            echo "✅ Health check passed - Function responds with 200"
          else
            echo "❌ Health check failed - Function responded with $response"
            exit 1
          fi

      - name: Performance test
        run: |
          DEPLOY_URL="${{ steps.deploy.outputs.url }}"
          if [ -z "$DEPLOY_URL" ]; then
            echo "No deploy URL available, skipping performance test"
            exit 0
          fi

          echo "Performance testing function endpoint..."
          time curl -s "$DEPLOY_URL/.netlify/functions/sync-jobs" > /dev/null
          echo "✅ Basic performance test completed"
